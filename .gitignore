# macOS
.DS_Store

# Private / draft material. The GitHub repo is PUBLIC (a GitHub Pages requirement on free accounts).
reference/
content/private/

# Local tool state
.claude/settings.local.json
node_modules/

# Original ad artwork (full-size PNGs with contact details). Compressed copies live in public/assets/img/ads/ (D32).
original-ad-images/

# PDF build output: JPEG copies of public/ images and the rendered page backgrounds (scripts/build-pdf.sh makes them).
pdf/img/
